server {
 listen 80 default_server;
        listen [::]:80 default_server;

{% for i in backends %}

 location /{{ i.name }} {
    proxy_pass http://{{ i.hostip }}:{{ i.hostport }}/;
  }
{% endfor %}

}


